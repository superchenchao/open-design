name: ci-gate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    name: CI gate placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "ci-gate placeholder"
