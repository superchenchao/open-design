name: ci-runner

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    name: Runner CI placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "ci-runner placeholder"
