# Self-hosted release-beta lane. Hosted baseline remains release-beta.yml.
name: release-beta-s

on:
  workflow_dispatch:
    inputs:
      enable_win:
        description: "Run the Windows self-hosted beta lane."
        required: true
        type: boolean
        default: true
      publish:
        description: "Publish release assets and metadata. Keep disabled while validating the runner lane."
        required: true
        type: boolean
        default: false
      release_version:
        description: "Local beta candidate version for no-publish self-hosted validation."
        required: true
        type: string
        default: "0.8.1-beta.0"

permissions:
  contents: read
  actions: read

concurrency:
  group: release-beta-s-win
  cancel-in-progress: false

jobs:
  build_win:
    name: Build Windows beta candidate
    if: ${{ inputs.enable_win }}
    runs-on: [self-hosted, Windows, X64, nexu-win, release-beta]
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2
        with:
          clean: false
          fetch-depth: 1
          lfs: false

      - name: Validate publish mode
        if: ${{ inputs.publish }}
        shell: cmd
        run: |
          @echo on
          powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -Command "throw 'release-beta-s publish mode is not wired yet; keep publish=false until the internal publisher is added'"

      - name: Build beta candidate
        shell: cmd
        run: |
          @echo on
          powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -File .github\scripts\release\build-beta.ps1 -Platform win -Lane self-hosted -Namespace release-beta-win -ReleaseVersion "${{ inputs.release_version }}" -SmokeMode fast -Target nsis

      - name: Publish local summary
        if: ${{ always() }}
        shell: cmd
        run: |
          @echo on
          echo workspace=%GITHUB_WORKSPACE%
          echo runner_temp=%RUNNER_TEMP%
          echo runner_tool_cache=%RUNNER_TOOL_CACHE%
          powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -Command "if ($env:GITHUB_WORKSPACE -like 'C:\Users\runner\Projects\*') { throw 'GITHUB_WORKSPACE must not point at the operator Projects directory' }"
          if exist "C:\.tmp\runner\od-beta\win\summary.md" echo local_summary=C:\.tmp\runner\od-beta\win\summary.md
